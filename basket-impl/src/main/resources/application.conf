play.modules.enabled += com.elmenus.basket.impl.BasketModule

lagom.persistence.ask-timeout = 10s

basket.cassandra.keyspace = basket

cassandra-journal.keyspace = ${basket.cassandra.keyspace}
cassandra-snapshot-store.keyspace = ${basket.cassandra.keyspace}
lagom.persistence.read-side.cassandra.keyspace = ${basket.cassandra.keyspace}

